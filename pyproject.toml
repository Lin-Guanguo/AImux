[project]
name = "aimux"
version = "0.1.0"
description = "AI-driven tmux session manager"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
aimux = "aimux:main"

[build-system]
requires = ["uv_build>=0.9.13,<0.10.0"]
build-backend = "uv_build"
